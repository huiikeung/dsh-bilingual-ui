/**
 * dsh-bilingual-ui — host half.
 *
 * This plugin is browser-only: it rewrites UI labels at runtime and needs no
 * host service, route, credential, or config. The cordis bundle row still
 * mounts the package, so this entry exists and deliberately does nothing.
 * @module
 */
export function apply() {}
